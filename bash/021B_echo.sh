# write the text:
# this is my "first time" and it won't be the last
# using all kind of echo style (if you can): no quote, double, single

# there are some environment variables already set for example:
# PATH
# HOME
# SHELL
# echo each one of them separately

# echo all of them in one command
