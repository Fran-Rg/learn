# 4 STDOUT
# This is the OUTPUT a program creates
# example:
echo "1234" # STDOUT = 1234
which grep # STDOUT should be something like "/usr/bin/grep"

# You can "redirect" an STDOUT to a file by doing:
PROGRAM 1> file.txt # The 1 means "STDOUT"
PROGRAM > file.txt
