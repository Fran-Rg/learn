# Declare one variable of each type

# Array access
starting_array = ["can", "you", "link", "ERROR", 666]
# Replace "ERROR" by "me"

# create a variable named "start" which contains the first value in the array
# create a variable named "end" which contains the last value in the array
# create a dictionnary "book" which for the key "start" contains the value of variable "start" and for the key "end" contains the value of variable "end"
